//Form with category, title, cost two buttons one to save and one to delete 
// puting data into state for Update
//the only thing that is udpated is the expense item 

//AddExpenseItem component above the updateList componenet 
    //items added gets added tos tate that shows in my trip 
    //adding the item saves it to the database at this point 
    //adding to state , which re-renders the expense list component 


    //parent component to hold Add Expense item and update expense Item and cost 
    //pass down expenseList state and setExpenseList function to udpateExpenseList

    //make a mock up and commponent tree 